{
  try {
    ((AsynchronousProcessor)r.cometSupport).cancelled(r.getRequest(),r.getResponse());
  }
 catch (  IOException e) {
    logger.debug("",e);
  }
catch (  ServletException e) {
    logger.debug("",e);
  }
}
