{
  Broadcaster b=f.get(UUID.randomUUID().toString());
  b.setBroadcasterLifeCyclePolicy(new BroadcasterLifeCyclePolicy.Builder().policy(IDLE_DESTROY).idleTime(10,TimeUnit.SECONDS).build());
  return new Broadcastable(b);
}
