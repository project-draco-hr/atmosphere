{
  logger.trace("calling from " + this.getClass().getName() + " : "+ "abort");
  try {
    webSocket.close();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  webSocket=null;
  session.onShutdown();
}
