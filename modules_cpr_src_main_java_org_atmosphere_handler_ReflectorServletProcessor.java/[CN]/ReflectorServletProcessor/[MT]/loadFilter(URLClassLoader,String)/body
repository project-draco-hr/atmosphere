{
  Filter f;
  try {
    f=(Filter)config.framework().newClassInstance(urlC.loadClass(fClass));
  }
 catch (  NullPointerException ex) {
    f=(Filter)config.framework().newClassInstance(Thread.currentThread().getContextClassLoader().loadClass(fClass));
  }
  return f;
}
