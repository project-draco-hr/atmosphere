{
  return new ServerTransportProtocol(){
    @Override void send(    String message,    AsyncCallback<Void> callback){
      socket.send(message);
      callback.onSuccess(null);
    }
    @Override String serialize(    Serializable message) throws SerializationException {
      return client.getSerializer().serialize(message);
    }
  }
;
}
