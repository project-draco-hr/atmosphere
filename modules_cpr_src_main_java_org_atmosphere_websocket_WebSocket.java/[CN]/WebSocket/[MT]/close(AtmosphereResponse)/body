{
  logger.trace("WebSocket.close()");
  if (r.request() != null && r.request().getAttribute(CLEAN_CLOSE) == null) {
    close();
  }
  try {
    bb.clear();
    cb.clear();
    buffer.close(r);
  }
 catch (  Exception ex) {
    logger.trace("",ex);
  }
}
