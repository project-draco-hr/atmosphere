{
  try {
    HttpEvent event=(HttpEvent)r.getRequest(false).getAttribute(HTTP_EVENT);
    if (event != null) {
      event.close();
    }
  }
 catch (  IOException ex) {
    logger.debug("",ex);
  }
  return this;
}
