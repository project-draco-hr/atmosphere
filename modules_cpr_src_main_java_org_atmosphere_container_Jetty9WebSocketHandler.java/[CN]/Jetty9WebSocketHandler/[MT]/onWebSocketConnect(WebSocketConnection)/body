{
  logger.trace("WebSocket.onOpen.");
  try {
    webSocketProcessor=WebSocketProcessorFactory.getDefault().newWebSocketProcessor(new Jetty9WebSocket(webSocketConnection,framework.getAtmosphereConfig()));
    webSocketProcessor.open(request);
  }
 catch (  Exception e) {
    logger.warn("Failed to connect to WebSocket",e);
  }
}
