{
  return new LinkedList<Class<? extends CometSupport>>(){
{
      if (testClassExists(JETTY_8))       add(Jetty8WebSocketSupport.class);
      if (testClassExists(GRIZZLY_WEBSOCKET))       add(GrizzlyWebSocketSupport.class);
    }
  }
;
}
