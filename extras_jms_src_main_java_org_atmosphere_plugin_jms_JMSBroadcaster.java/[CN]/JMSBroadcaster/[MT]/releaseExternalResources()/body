{
  try {
    connection.close();
    consumerSession.close();
    publisherSession.close();
    consumer.close();
    publisher.close();
  }
 catch (  Throwable ex) {
    logger.warn("releaseExternalResources",ex);
  }
}
