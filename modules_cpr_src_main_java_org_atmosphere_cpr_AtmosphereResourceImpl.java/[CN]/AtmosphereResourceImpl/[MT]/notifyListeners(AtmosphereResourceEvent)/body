{
  if (event.isResuming() || event.isResumedOnTimeout()) {
    onResume(event);
  }
 else   if (event.isCancelled()) {
    onDisconnect(event);
  }
 else   if (event.isSuspended()) {
    onSuspend(event);
  }
 else {
    onBroadcast(event);
  }
}
