{
  this.config=config;
  if (handleExecutors) {
    configExecutors();
  }
  configureBroadcasterFilter(list);
  configureBroadcasterCache();
  this.name=name;
  this.handleExecutors=handleExecutors;
}
