{
  if (path.contains("*")) {
    path=path.replace("*","[a-zA-Z0-9-&.=;\\?]+");
  }
  if (path.endsWith("/")) {
    path=path + "[a-zA-Z0-9-&.=;\\?]+";
  }
  atmosphereHandlers.put(path,w);
  return this;
}
