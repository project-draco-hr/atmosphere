{
  String response="{" + "\"win\": \"" + win() + "\", \"board\": [";
  for (int i=0; i < 9; i++) {
    response=response + "\"" + board[i]+ "\"";
    if (i < 8) {
      response=response + ",";
    }
  }
  response=response + "]," + "\"turn\": "+ whoseTurn()+ " }";
  return response;
}
