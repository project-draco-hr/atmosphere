{
  return new Runnable(){
    public void run(){
      try {
        incomingBroadcast();
      }
 catch (      Throwable t) {
        logger.debug("incomingBroadcast Exception. Broadcaster will be broken unless reconfigured",t);
        destroy();
        return;
      }
    }
  }
;
}
