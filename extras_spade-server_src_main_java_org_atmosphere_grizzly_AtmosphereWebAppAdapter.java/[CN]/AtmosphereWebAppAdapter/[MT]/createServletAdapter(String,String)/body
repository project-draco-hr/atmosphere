{
  AtmosphereAdapter adapter=new AtmosphereAdapter();
  adapter.setHandleStaticResources(true);
  adapter.setContextPath(context);
  adapter.setServletPath(getServletPath(servletUrlPattern));
  return adapter;
}
